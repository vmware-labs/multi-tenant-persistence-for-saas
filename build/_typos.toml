[default.extend-identifiers]
# *sigh* this just isn't worth the cost of fixing
importas = "importas"

[default.extend-words]
# Accepted words to be ignored by typos

[files]
extend-exclude = [".golangci.yml"]
